# Keepic 포토북 편집기 — Claude 작업 인수인계 문서

> 이 문서는 이전 대화를 모르는 새 Claude 세션(터미널 Claude Code든, Cowork든)이 이
> 저장소에서 바로 작업할 수 있도록 만든 참고 자료예요. 혜민님이 GPT와 함께 정리한
> 초안에 Cowork 세션에서 실제로 반복해온 작업 방식·규칙·아키텍처 사실을 더해서
> 만들었어요(2026-09-26). **아래 "과거 요구사항"·"로드맵" 항목은 현재 코드에 이미
> 반영됐을 수 있어요 — 항상 현재 코드부터 확인하고, 이번에 실제로 요청받은 항목만
> 작업하세요.**

## 프로젝트

- 서비스명: **Keepic** (K만 대문자). 사진으로 포토북·액자·굿즈를 제작하는 서비스.
- 작업 대상: 포토북 편집기(`app/upload/page.tsx`, 8,000줄 넘는 단일 파일). 표지·내지
  편집, 페이지 썸네일, 사진·텍스트·스티커 배치, 인쇄 미리보기 및 인쇄용 PDF
  (`lib/printCompose.ts`)가 관련돼요.
- 혜민님은 **디자이너이고 개발 지식이 없어요** — 항상 쉬운 말로 설명하고, 기술 용어는
  풀어서 써주세요. 요청은 보통 스크린샷 + 짧은 한 줄 피드백으로 옵니다.
- `AGENTS.md`/`CLAUDE.md`는 Next.js가 자동 생성하는 보일러플레이트예요(실제 프로젝트
  메모 아님) — 이 파일이 진짜 인수인계 문서예요.

## 제품 방향: 스위트북을 사용성 참고 대상으로

- 혜민님은 스위트북(Sweetbook)처럼 "디자인을 고르고 사진을 넣은 뒤 누구나 쉽게 책을
  완성하는 경험"을 참고해요. 화면 캡처를 참고 이미지로 자주 보내주세요 — **그대로
  복제하라는 뜻이 아니라 사용성·아이콘 스타일의 참고**예요. Keepic 고유 브랜드 색상·
  서체·인쇄 사양 기준으로 독자적인 UI를 만들어요.
- 지향하는 흐름: 템플릿 선택 → 사진 업로드 → 자동 배치 초안 → 직접 수정 → 인쇄
  미리보기 → 저장·제작 신청. 자동 배치("AI 맞춤 레이아웃")가 이미 완성됐다고 가정하지
  마세요 — 항상 구현 상태를 코드에서 확인하세요.
- 편집기는 중앙의 책/페이지가 주인공, 도구는 필요한 순간에만 보이게. 불필요하게
  중첩된 카드·두꺼운 테두리·반복된 라운드박스는 줄여요(2026-09-24 각짐 스타일 정리,
  2026-09-24~ 다른 세션이 커밋한 rounded-corner 전면 제거 참고). 작업선(재단·도련·
  안전·접힘·제본)은 편집 중 유지하고 인쇄 미리보기와 분명히 구분해요.
- 모바일 지원은 아직 별도 검토 대상이에요(데스크톱 편집 기능을 억지로 욱여넣지 말 것).

### 로드맵 — 한 번에 다 만들지 말 것, 요청받은 항목만

1. **템플릿·테마**: 가족/여행/커플/반려동물/키즈·졸업 등 표지·내지 샘플 미리보기·선택.
   테마 전환 시 사용자가 넣은 사진·글 보존 규칙 필요.
2. **사진 관리**: 다중 업로드, 사진 목록, 빈 프레임 채우기, 드래그로 페이지에 넣기,
   기존 사진 교체, 업로드 실패·저화질 안내.
3. **페이지 레이아웃**: 칸 수·배열 변경, 프레임 추가/삭제, 사진과 프레임 독립 조절,
   레이어 순서(삭제·앞으로·뒤로·맨앞·맨뒤) — **이 중 레이어 순서·확대/축소/회전/
   좌우반전/투명도/테두리는 2026-09-26 라운드에서 이미 구현했어요** (아래 아키텍처
   참고). 아직 없는 건: 마스크(사진틀모양), 테두리 프리셋/스타일링 고도화.
4. **꾸미기 자산**: 스티커/손글씨 구분(이미 있음), 소품·문구·캐릭터·라벨·아이콘·
   스티커글자·졸업증/상장 등 세부 카테고리 탐색(아직 없음).
5. **편집 지속성**: 자동 저장·이어서 편집·실행취소/다시실행(있음)·페이지 순서 변경·
   복제(상태 확인 필요).
6. **최종 점검**: 표지·책등·내지 순서, 잘리는 사진/텍스트, 빈 프레임, 화질, 재단·
   안전영역 점검 + 실제 인쇄에 가까운 미리보기(부분적으로 있음, `isPrintPreview`).

## 새 세션 첫 작업 순서

1. 이 문서와 (Cowork 세션이면) claude.ai 프로젝트 "Keepic 프로젝트"의
   `claude/print-pdf-generator-progress.md`(라운드별 상세 이력)·
   `claude/homepage-plan.md`를 먼저 읽어요. 터미널 세션이면 이 문서와 최근 git log가
   유일한 기록이에요.
2. `git log --oneline -20`으로 최근 커밋과 실제로 뭐가 반영됐는지 먼저 확인 — 이
   저장소는 **여러 Claude 세션(터미널 Claude Code, Cowork)이 번갈아 작업**하니
   과거 대화 기억만 믿지 말고 항상 현재 코드·커밋을 기준으로 판단해요.
3. 이번 요청에 필요한 최소 범위만 수정하고, 표지·내지 양쪽에서 기존 기능이 안
   깨졌는지 확인.
4. 변경 파일·확인 결과를 짧게 보고. **코드/명령 실행 없이 "수정 완료"라고 말하지
   않기.**

## 이 저장소에서 반드시 지킬 작업 규칙 (Cowork 세션 기준, 2026-09 누적)

- **절대 `git push` 하지 않기.** 혜민님이 본인 Mac 터미널에서 직접 push해요. push까지
  끝났는지는 매번 확인하지 말고, 로컬에 새 커밋이 쌓여있으면 보고 시 "push 필요"라고
  알려주면 돼요.
- Cowork 세션은 `mcp__remote-devices__device_bash`로만 이 저장소(혜민님 맥의
  `~/Desktop/hm38-photo-studio`, 이 문서 기준 `$HOME/mnt/hm38-photo-studio`)를
  건드릴 수 있어요 — 클라우드 쪽 Read/Edit/Write/Bash 도구는 이 폴더에 닿지 않아요.
- **라이브 브라우저·개발 서버를 직접 볼 수 없어요.** 검증은 `npx tsc --noEmit`,
  `npx eslint <파일>`, 코드 읽기/grep/교차확인, 필요하면 `npx tsx`로 만든 별도
  기하 계산 검증 스크립트까지만 가능해요. 시각적/인터랙티브/PDF 확인이 필요한 항목은
  "제가 확인 못 했어요, 혜민님이 확인해주세요"라고 명확히 구분해서 말하기 — 확인 안 된
  걸 확인됐다고 말하지 않기.
- 파일 수정은 python으로 `src = src.replace(old, new)` 패턴 사용,
  `assert src.count(old) == 1`로 유일성 확인 후 전체 `src`를 다시 씀(절대 diff
  조각만 파일로 쓰지 않기). 수정 후 `wc -l`/`git diff --stat`으로 정합성 확인.
- **커밋마다 tsc + eslint(28-problem 기준선 — 2 errors는 기존 `<a>` 태그·26개는
  `<img>` 관련 경고, 전부 이번 작업과 무관한 기존 것) 재확인.** 새 unused-var
  경고가 생기면 그 자리에서 정리.
- git lock 잔여 파일(`.git/index.lock`, `.git/HEAD.lock` 등)은 `ps aux | grep git`으로
  실제 실행 중인 git 프로세스가 없는지 먼저 확인한 다음, `rm` 대신 `_to_delete/`로
  `mv`해서 치워요(삭제 권한을 명시적으로 받은 게 아니면 `rm` 금지).
- **2026-09-26에 다른 로컬 Claude Code 세션(Haiku 4.5)이 같은 파일을 동시에 건드려
  커밋(`4e40346`)까지 한 적이 있어요.** 혜민님은 그 세션을 "멍청해서 안 쓰겠다"고
  하셨지만, 앞으로도 여러 세션이 겹칠 수 있으니 작업 시작 전에 `git status`로
  unstaged/staged 변경이 이미 있는지, `git log`로 최근 커밋 작성자·세션 ID가 이번
  세션과 다른지 확인하는 습관을 들이는 게 안전해요.
- 커밋 메시지 끝에 아래 서명 포함(Cowork 세션 기준 — 세션 ID는 실제 값으로):
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_015ff75CZXsUzDxWnngCjiSU
  ```

## 핵심 아키텍처 요약 (2026-09-26 기준)

- **`lib/albumTemplates.ts`**: `ImageBoxDef`(자유 배치 사진/스티커 박스), `TextBoxDef`
  (자유 배치 텍스트박스) 타입 정의. 둘 다 `zOrder?`로 쌓임 순서를 공유(레이어 기능).
  `ImageBoxDef`는 `kind?: "photo" | "sticker"`로 사진/스티커를 구분하고,
  `isStickerImageBox(box)` 헬퍼로 하위호환 판별(kind 없으면 url이 `/stickers/`로
  시작하는지로 판단). 2026-09-26에 `rotation?`(화면 전용, 인쇄 미반영)·`opacity?`·
  `borderWidthPx?`·`borderColor?` 필드 추가.
- **좌표계**: 텍스트박스는 왼쪽/오른쪽 낱장 각각(0~100%), 이미지박스는 스프레드
  전체 폭 기준(0~100%, 책등을 자유롭게 넘나들 수 있음). 이 차이 때문에 두 종류의
  다중 선택·정렬 로직이 따로 존재함.
- **`ImageBoxOverlay`** (app/upload/page.tsx): 사진박스 하나의 화면 렌더링·드래그·
  리사이즈·더블클릭 시 "사진 위치 조정 모드" 진입. `computeImageBoxCoverRect`
  (`lib/imageBoxGeometry.ts`)로 박스를 항상 꽉 채우는 crop 계산 — 화면과 인쇄
  파일이 이 함수를 공유해서 항상 일치함. 스티커는 이 crop 계산을 안 쓰고
  `object-contain`으로 통째로 보여줌(잘리지 않게).
- **`StackOrderToolbar`**: 박스 선택 시 캔버스 위에 뜨는 작은 아이콘 툴바.
  2026-09-26에 텍스트 기호(« ‹ › » ✕)에서 `LayerIcon` 선 아이콘 세트로 전면 교체.
  `mediaKind`("text"|"photo"|"sticker")로 버튼 구성이 갈림 — 텍스트는 삭제·맨앞·맨뒤,
  사진은 맞춤·확대·축소·회전·좌우반전·투명도·테두리·편집까지, 스티커는 크롭 개념
  없는 맞춤·편집만 뺀 세트.
- **다중 선택 정렬**: 텍스트박스(`multiTextSelection`, 2026-09~)와 이미지박스
  (`multiImageSelection`, 2026-09-26~)가 각각 별도 상태로 존재. Shift+클릭으로
  토글, 2개 이상이면 정렬 UI가 뜸(텍스트는 왼쪽 패널 폼, 이미지는 캔버스 위 떠있는
  아이콘 툴바 `MultiAlignFloatingToolbar`). 정렬 기준은 항상 "선택된 박스들의
  바운딩 박스". 이미지박스는 이번 라운드에 **내지 스프레드 안만** 지원 — 표지
  칸(`coverImageBoxes`/`backCoverImageBoxes`)은 아직 다중 선택 미지원.
- **인쇄 파일**: `lib/printCompose.ts`의 `drawImageBoxOnCanvas`(내지, 페이지 경계
  클리핑 포함)·`drawImageBoxesInPanel`(표지 레이아웃 칸, 경계 없음)가 실제 PDF
  캔버스에 그림. `PRINT_DPI = 300`, 화면은 `CSS_PX_PER_MM = 96/25.4` 기준이라
  화면 px→인쇄 px 환산 시 `PRINT_DPI/96` 배율을 곱함(테두리 두께 등).
- **`editorMode`**: `"preview" | "edit"`. 2026-09-24 원래 규칙(편집 중엔 페이지
  목록 숨김, 미리보기에서만 보임)을 2026-09-26 초반에 데스크톱만 항상 보이게
  바꿨다가(`5d75370`), 같은 날 후속 라운드에서 "박스가 너무 많아 조잡하다"는 피드백
  + "편집하기 누르면 페이지 목록 사라지고 편집 메뉴만 보이게" 요청으로 **다시
  미리보기 전용으로 되돌림**(`bfa4819`) — 지금은 데스크톱·모바일 모두
  `editorMode === "preview"`일 때만 페이지 목록이 보여요. 다만 **보일 때의 모양은
  유지**: 데스크톱은 왼쪽 세로 사이드바(`lg:w-28 lg:flex-col`), 모바일은 하단 가로
  바(예전 방식 그대로). 편집 중 페이지 이동은 대신 **편집 캔버스 좌우의 `‹`/`›`
  플로팅 버튼**(`3755f42` 신규)으로 함 — 왼쪽 사이드바와 같은 `pageOrder` 이동
  로직 재사용.
- **표지 "테마" 탭**(`ef32b3e`, 2026-09-26 신규): `COVER_EDIT_TABS`의 첫 탭.
  `COVER_THEMES` 배열(현재 화이트 심플/웜 베이지 2개, 각각 프론트·책등·백 배경색 +
  백커버 무늬(`lib/backgroundPatterns.ts` 재사용, 선택) + 제목 서체 세트)을
  `applyCoverTheme(themeId)`로 한 번에 적용 — **기존 사진·텍스트박스는 전혀 안
  건드리는 비파괴적 적용**(레이아웃 템플릿 적용과 같은 원칙). "앞표지/뒤표지 낱장
  메뉴가 필요 없어진다"는 원 요청은, 그 메뉴 자체를 지우지 않고 테마로 한 번에
  세팅 + 필요하면 세부 조정도 그대로 가능하게 구현 — 기존 커스터마이징 기능과
  이미 작업 중인 프로젝트를 해치지 않기 위한 선택. **주의: `COVER_THEMES`는 반드시
  `fontOptions` 배열 선언 뒤에 있어야 함**(기본 서체를 `fontOptions[n].id`로 참조 —
  선언 순서를 어기면 TS "used before declaration" 에러). 표지에 스티커를 붙이는
  기능 자체가 아직 없어서, 테마별 데코 스티커는 이번엔 제외(아래 미해결 항목 참고).
- **배경 패널(그래픽/패턴/텍스처/단색)**: `lib/backgroundPatterns.ts` 기반으로
  **표지·내지 모두 이미 완전히 구현되어 있음**(내지는 `activeEditTab ===
  "background"`, 표지는 `activeCoverEditTab === "background"`). 2026-09-26에
  혜민님이 "배경 패널을 새로 만들어달라"고 요청했었는데 조사 결과 이미 구현돼
  있어서 새 코드 작성 없이 위치만 안내함 — 새 세션이 이 요청을 다시 받으면 먼저
  코드부터 확인할 것.
- **레이어(쌓임) 순서**: `effectiveZOrder()`(사진/텍스트 종류 상관없이 섞어서 순서
  매김), `computeZOrderUpdates()`로 앞으로/뒤로/맨앞/맨뒤 계산.
  `applySpreadStackAction`(내지)/`applyCoverStackAction`(표지)이 실제 적용.

## 알려진 미해결/확인 필요 항목

- **독수리 스티커가 "사진" 탭으로 튕기는 버그**(2026-09-24 보고): 코드 로직상으로는
  재현 안 돼야 하는데 혜민님이 재현하셨다고 보고. 하드 리프레시/개발서버 재시작 후
  재현 여부 확인 요청드린 상태 — 후속 보고 대기 중.
- **"커다란 외곽 라운드박스 제거"**: 2026-09-26에 완전히 해결됨. 둥근 모서리는
  `4e40346`에서 이미 제거됐었고, 남아있던 표지/소개 페이지/내지 편집 카드의 직선
  `border`(하늘색 테두리)는 `0740b36`에서 제거(3곳 모두), 하단 썸네일 바의
  `border-t` + 두꺼운 기본 스크롤바로 "박스 안에 박스"처럼 보이던 문제는 `8265a34`
  에서 해결(`scrollbarWidth: "thin"`). 혜민님이 첨부한 두 스크린샷의 검은 동그라미
  두 곳 모두 처리 완료.
- **사진박스 회전은 인쇄 파일에 미반영**(화면 전용) — 다음에 제대로 하려면 스프레드
  경계를 넘나드는 박스가 양쪽 낱장 캔버스에 나눠 그려지는 기존 클리핑 로직과 회전을
  함께 계산해야 해서 별도 라운드로 잡을 것.
- **표지 칸 이미지박스 다중 선택 정렬 미지원** — 내지와 같은 패턴으로 확장 가능,
  요청 시 진행.
- **스위트북 참고 전체 화면 구조 개편 — 진행 중, 여러 라운드째 반복 수정됨**:
  혜민님이 스위트북 편집기 캡처(왼쪽 세로 페이지 목록, 상단 미리보기/장바구니/
  저장하기, 캔버스 옆 플로팅 화살표, 하단 정보 스트립, 중앙 배치)를 계속 참고로
  보내주셔서 라운드마다 항목별로 조금씩 반영 중. 지금까지: 편집모드 전환 버튼 상단
  중앙 배치·캔버스 좌우 플로팅 화살표(`3755f42`), 표지/소개 페이지/내지 카드
  `bg-white` 배경 제거(`46c5af7`), 왼쪽 페이지 목록은 스타일만 왼쪽 세로로 바꾸고
  표시 조건은 미리보기 전용으로 재확정(`5d75370`→`bfa4819`), 표지 테마 탭(`ef32b3e`).
  **아직 안 한 것**: (1) 상단바 "장바구니"/"저장하기" 버튼 — 기존
  "다음"(`handleProceed`)이 이미 진짜 저장+진행 동작을 하고 있어서, 이걸 재활용할지
  별도 버튼을 새로 만들지 확인 필요. 가짜 버튼은 절대 만들지 말 것(2026-09-23 원칙).
  (2) 하단 정보 스트립(상품/사진/제목·만든이/테마/페이지 "변경") — 각 "변경"이 실제로
  연결될 화면이 있어야 구현 가능, 없으면 가짜 링크가 되니 먼저 확인. (3) "스위트북처럼
  박스 최소화, 선으로 표처럼 나누기" — 앱 전체 컴포넌트에 영향을 주는 광범위한
  재스타일링이라 아직 범위를 좁혀서 협의 안 함. (4) 표지에 스티커를 붙이는 기능
  자체가 없어서, 표지 테마에 데코 스티커를 넣으려면 이 기능부터 만들어야 함.

## 기록 위치

- 라운드별 상세 작업 이력(무엇을 왜 어떻게 고쳤는지, 검증 메모 포함)은 claude.ai
  "Keepic 프로젝트"의 `claude/print-pdf-generator-progress.md`에 계속 쌓고 있어요
  (Cowork 세션 전용 — 터미널 세션은 git log와 이 문서, 코드 주석이 기록의 전부예요).
- 홈페이지(판매 페이지) 관련 계획은 같은 프로젝트의 `claude/homepage-plan.md`.
